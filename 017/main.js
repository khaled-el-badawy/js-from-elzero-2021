let myTitle = "khaled",
  artDiscription = "elzero web school",
    aetDate = "25/10";
  
let card = `
<h3>Hello ${myTitle}</h3>
<p>${artDiscription}</p>
<span>${aetDate}</span>
`;

document.writeln(card, card, card, card);


