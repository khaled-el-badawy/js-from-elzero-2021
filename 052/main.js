let products = ["Keyboard", "Mouse", "Pen", "Pad", "Monitor", "iPhone"];

let i = 0;
for (;; ) {
  console.log(products[i]);
  // i++;
  i += 1;
  if (i === products.length) break;
}

// console.log(i);
